version: "3.9"

services:
  frontend_app:
    container_name: prostoemenu-frontend-container
    image: prostoemenu-frontend
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env