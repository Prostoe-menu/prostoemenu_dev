import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const cookingStepsSlice = createSlice({
  name: 'cookingSteps',
  initialState,
  reducers: {},
});

export default cookingStepsSlice.reducer;
