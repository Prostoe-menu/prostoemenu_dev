import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const ingrediensSlice = createSlice({
  name: 'ingredients',
  initialState,
  reducers: {},
});

export default ingrediensSlice.reducer;
